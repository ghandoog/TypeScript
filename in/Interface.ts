export interface Person {
    name: string;
    age: number;
}

let newPerson: Person = {
    name: 'John',
    age: 30
}

// make, model, and year

export interface Car {
  make: string;
  model: string:
  year?: number;
}

let newCar: Car = {
  make: 'Honda',
  model: 'Accord'
}